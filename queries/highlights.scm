;;; TODO: list all highlight variables used (the ones prefixed with @).

(comment) @comment

(symbol) @variable

(string) @string
(number) @number

[ "'" "`" "," ",@" ] @operator

["(" ")"] @punctuation.bracket

